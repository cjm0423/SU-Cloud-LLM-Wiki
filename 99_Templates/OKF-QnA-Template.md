---
title: "[증상 — 원인/결론을 제목에 담는다]"
# 좋은 예: "개발계(.180) 네트워크 장애 — 원인 확정 및 MAC 스푸핑 임시 조치"
# 나쁜 예: "네트워크 문제" (검색해서 찾을 수 없다)
type: "qa"
# type 값: qa (질의응답) | troubleshooting (장애 해결)
date: YYYY-MM-DD
tags: ["#networking", "#troubleshooting", "#devpc"]
# TAXONOMY.md의 23개 통제 어휘에서만 고른다. 3~5개.
status: "open"
# status 값: open (미해결) | resolved (해결됨) | wontfix (해결 안 함)
related_nodes: ["[[연관 개념 문서]]", "[[이전 트러블슈팅 문서]]"]
author: "AI Assistant & [작성자 이름]"
raw_source: "[[00_Inbox/원본 파일명]]"

# --- 발생 환경 ---
environment: "[예: Gaming5 개발계 / Ubuntu 24.04 / OpenStack 2026.1]"
# --- 검색용 핵심 키워드 (에러 코드, 서비스명 등) ---
symptoms: ["[예: ModuleNotFoundError]", "[예: sphinx 버전 충돌]"]
---

# [제목]

## 1. 증상

**무엇이 안 되는가** (관측된 사실만. 추측은 아래 분석에 쓴다)

- (예: VM에서 8.8.8.8 ping 불가. 게이트웨이까지는 도달)

**에러 메시지 원문**

> 요약하지 말고 그대로 붙여넣는다. 나중에 같은 에러를 검색으로 찾는 통로다.

```
(로그/에러 전문 그대로)
```

**재현 조건**

- (항상 발생 / 특정 조건에서만 / 재부팅 후에만 등)

## 2. 환경

| 항목 | 값 |
|---|---|
| 호스트 | (예: Gaming5, 192.168.x.180) |
| OS | |
| 관련 버전 | |
| 발생 시점 | YYYY-MM-DD |

## 3. 분석 및 추론

(가설 → 검증 순서로. 어떤 근거로 원인을 좁혀갔는가)

| # | 가설 | 검증 방법 | 결과 |
|---|---|---|---|
| 1 | | | ❌ 기각 |
| 2 | | | ✅ 확인 |

**근본 원인:** (한 문장으로 확정)

## 4. 시도했지만 실패한 방법

> **이 섹션을 비우지 않는다.** 같은 함정에 다시 빠지는 것을 막아준다.
> 실패한 시도가 없었다면 "없음"이라고 적는다.

| 시도 | 왜 실패했는가 |
|---|---|
| (예: netplan에 DNS 직접 지정) | DNS가 아니라 L2 차단이 원인이라 무관 |

## 5. 해결책

**조치 성격:** (근본 해결 / 임시 조치(workaround) / 회피)

> 임시 조치라면 **왜 임시인지, 근본 해결은 무엇인지** 반드시 적는다.

```bash
# 적용한 명령어
```

**해결 확인:**

```bash
# 해결됐음을 증명하는 명령어와 출력
```

## 6. 재발 방지

- (예: 학내망 신규 장비는 MAC 등록 먼저 — [[03_Guides/Campus-Network-Runbook]]에 반영)
- (모니터링 추가, 가이드 문서 갱신, 체크리스트 추가 등 구체적 후속 조치)

## 7. 남은 과제

- [ ] (근본 해결이 안 됐다면 무엇이 남았는지)

## 관련 문서

- 개념: [[01_Concepts/...]]
- 가이드: [[03_Guides/...]]
- 유사 사례: [[02_QnA_Archive/...]]
